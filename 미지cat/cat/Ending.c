#define _CRT_SECURE_NO_WARNINGS

#include <stdio.h>
int main() {
	int score = 0;
	if (-23 <= score && score <= 10) {
		printf("평소보다 진상이 많아 참지 못하고 진상 손님의 얼굴을 때려버렸다...\n");
		printf("헉... 점장이 내일부터 나오지 않아도 된다고 했다.\n");
		printf("쓸쓸하게 집으로 돌아간다... -> 이제 두번다시 고양이를 보지 못하게 되었다.\n");
	}

	else if (11 <= score && score <= 22) {
		printf("힘들었던 하루가 끝났다. 집으로 얼른 가야지~\n");
		printf("(고양이가 짠~~)\n");
		printf("저녁에 보이지 않았는데 오늘은 웬일이지? -> 고양이가 달려와서 플레이어 품에 안긴다.\n");
	}

	else if (23 <= score && score <= 31) {
		printf("힘들었던 하루가 끝났다. 집으로 얼른 가야지~\n");
		printf("(고양이가 짠~~)\n");
		printf("저녁에 보이지 않았는데 오늘은 웬일이지? -> 고양이가 달려와서 플레이어 품에 안긴다.\n");
	}


	int coice = 0;
	coice += 1;
	if (coice = 1) {
		printf("고양이를 따라가보면... 저 멀리 작은 고양이가 보인다.\n");
		printf("상태를 살펴보니 아픈 것 같다. 근처의 동물병원으로 가 치료를 해주자 -> 두마리의 고양이를 키우게 된다. \n");
	}
}
